@model IEnumerable<API_Notas.Models.Usuario>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Contraseña)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RutPersonaNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Contraseña)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RutPersonaNavigation.RutPersona)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NomUsuario">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.NomUsuario">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.NomUsuario">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
